import 'dart:ui';

class Colors {
    static const white = Color.fromRGBO(255, 255, 255, 1);

    static const red = Color.fromRGBO(255, 0, 0, 1);

    static const black = Color.fromRGBO(0, 0, 0, 1);
    static const darkGray = Color.fromRGBO(50, 50, 50, 1);
    static const gray = Color.fromRGBO(150, 150, 150, 1);
    static const lightGray = Color.fromRGBO(200, 200, 200, 1);
}